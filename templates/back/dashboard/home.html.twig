{% extends 'back/base.html.twig' %}

{% block title %} Bienvenue sur le BackOffice {% endblock %}

{% block body %}
    <main>
        <div class="container mt-5 ">
        <div class="container mt-5 ">
            <div class="row mt-5 p-4 bg-body-secondary  border text-center rounded">
                <h2 class="text-muted fw-bold" >BackOffice - Administrateur</h2>
            </div>
            
            <div class="row mt-5 p-4 border text-center rounded">
                <div class="col-md-5 bg-body-secondary border mx-auto p-5 mt-4 rounded">
                    <h3 class="text-muted fw-semibold">Gestion des Stagiaires</h3>
                    <a href="{{ path('app_back_intern_list') }}" class="btn btn-secondary mt-4 text-center" role="button">
                        Liste des Stagiaires
                    </a>
                </div>

                <div class="col-md-5 bg-body-secondary border mx-auto p-5 mt-4 rounded">
                    <h3 class="text-muted fw-semibold">Gestion des Entreprises</h3>
                    <a href="{{ path('app_back_company_list') }}" class="btn btn-secondary mt-4 text-center" role="button">
                        Liste des Entreprises
                    </a>
                </div>

                <div class="col-md-5 bg-body-secondary border mx-auto p-5 mt-4 rounded">
                    <h3 class="text-muted fw-semibold">Gestion des Offres</h3>
                    <a href="{{ path('app_back_offer_list') }}" class="btn btn-secondary mt-4 text-center" role="button">
                        Liste des offres de stage
                    </a>
                </div>

                <div class="col-md-5 bg-body-secondary border mx-auto p-5 mt-4 rounded">
                    <h3 class="text-muted fw-semibold">Gestion des Administrateurs</h3>
                    <a href="{{ path('app_back_admin_list') }}" class="btn btn-secondary mt-4 text-center" role="button">
                        Liste des Administrateurs
                    </a>
                </div>
            </div>
        </div>
        </div>
    </main>  
{% endblock %}
